<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_gravity="center"
    android:background="@drawable/product_item_bg"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="15dp">

    <ImageView
        android:id="@+id/img_product_item"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:src="@drawable/shoes_png" />


    <TextView
        android:id="@+id/tv_title_product_item"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textAlignment="center"
        android:textColor="#C4000000"
        android:textSize="15sp"
        android:textStyle="bold"
        tools:text="Shoes_Name" />

    <TextView
        android:id="@+id/tv_price_product_item"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="5sp"
        android:textAlignment="center"
        android:textColor="#C4000000"
        android:textSize="15sp"
        android:textStyle="italic"
        tools:text="00, 00 $" />


    <LinearLayout
        android:id="@+id/ll_btn_item"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginTop="5sp"
        android:gravity="center"
        android:orientation="horizontal">

        <Button
            android:id="@+id/btn_add_cart_product_item"
            android:layout_width="wrap_content"
            android:layout_height="30dp"
            android:background="@drawable/btn_bg"
            android:drawableStart="@drawable/ic_add_shopping_cart"
            android:drawableLeft="@drawable/ic_add_shopping_cart"
            android:focusable="false"
            android:text="@string/add_cart_text" />

        <Button
            android:id="@+id/btn_setting_product_item"
            android:layout_width="wrap_content"
            android:layout_height="30dp"
            android:background="@drawable/btn_bg"
            android:drawableStart="@drawable/ic_settings"
            android:drawableLeft="@drawable/ic_settings"
            android:focusable="false"
            android:text="@string/setting_text" />
    </LinearLayout>
</LinearLayout>